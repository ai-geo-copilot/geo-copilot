services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: geo
      POSTGRES_USER: geo
      POSTGRES_PASSWORD: geo
    ports:
      - "5432:5432"
    volumes:
      - geo-postgres:/var/lib/postgresql/data

volumes:
  geo-postgres:
